import React, { Fragment } from 'react';

import BlogSideDrawer from './blog-side-drawer';

const Blog = () => {
	return (
		<Fragment>
			<BlogSideDrawer />
		</Fragment>

	);
};

export default Blog;